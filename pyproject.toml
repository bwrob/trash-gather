[project]
name = "trash-gather"
version = "0.1.0"
description = "C garbage collector & VM runtime — explorations in automatic memory management"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
# Enable: pycodestyle (E/W), pyflakes (F), isort (I), bugbear (B),
#         simplify (SIM), naming (N), pyupgrade (UP)
select = ["E", "W", "F", "I", "B", "SIM", "N", "UP"]
ignore = [
    "E501",  # line too long — diagnostic message strings are intentionally verbose
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pyrefly]
# Only check project scripts, not vendor/venv
project-includes = ["scripts/**/*.py"]
preset = "strict"

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "pyrefly>=1.2.0",
    "ruff>=0.16.6",
]
